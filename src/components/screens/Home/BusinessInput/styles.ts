import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 15,
    // marginBottom: 15,
    // borderRadius: 25,
  },
  input: {
    borderWidth: 1,
    // borderRadius: 25,
    borderColor: '#ffff',
  },
  card: {
    marginTop: 10,
  },
});
